console.log("main.js conectado correctamente");

// Buscamos el elemento <h1> en el HTML
const titulo = document.querySelector("h1");

// Le cambiamos el texto
titulo.textContent = "¡Bienvenido a SyA! (modificado por JS)";